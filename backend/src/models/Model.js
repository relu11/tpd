class Model {
  constructor(tableName) {
    this.tableName = tableName;
  }

  getALl() { /* get ALl */ }

  insert() { /* inserts to database */ }

  get(_columns, _conditions) { /* Gets elements from database */ }
}

export default Model;
